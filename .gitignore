# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Dependencies
node_modules
dist
dist-ssr
*.local

# Python
.venv/
venv/
__pycache__/
*.pyc
*.pyo

# Runtime uploads (namecard images, pending files)
backend/uploads/
*.egg-info/

# Editor
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Backups
*.bak
*.backup

# Environment
.env
backend/.env

# ── Secrets / keys：一律唔入 git（2026-09-15 AppScan 修正）────────────────
# RS256 JWT keypair 留喺 backend/keys/（0600，路徑由 NEXUS_JWT_PRIVATE_KEY_PATH 控制）
*.pem
*.key
backend/keys/
.secrets/
backend/.secrets/
userlist.txt
.git-credentials

# App-level encryption key (NEXUS_SECRET_KEY in .env)
backend/app/keys/secret.key
backend/app/oauth_clients.json
.pytest_cache/

# opencv.js copied at build time (13MB — from node_modules)
public/vendor/

# Per-deployment secrets — never commit
backend/keys/
backend/.env
backend/.secrets/
*.pem
